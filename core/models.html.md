# models


<!-- WARNING: THIS FILE WAS AUTOGENERATED! DO NOT EDIT! -->

## ScrollbarConfig

------------------------------------------------------------------------

### ScrollbarConfig

``` python

def ScrollbarConfig(
    prefix:str='sb', show_scrollbar:bool=True, min_thumb_height:int=24, track_width:int=3, auto_hide:bool=True
)->None:

```

*Initialization-time configuration for a virtual scrollbar.*

## ScrollbarState

------------------------------------------------------------------------

### ScrollbarState

``` python

def ScrollbarState(
    position:int=0, visible_count:int=1, total_items:int=0, max_position:Optional=None, thumb_ratio:Optional=None
)->None:

```

*Mutable runtime state for a virtual scrollbar.*

## ScrollbarIds

------------------------------------------------------------------------

### ScrollbarIds

``` python

def ScrollbarIds(
    prefix:str='sb'
)->None:

```

*HTML element ID generators for a scrollbar instance.*

## Tests

``` python
# Default config
cfg = ScrollbarConfig()
assert cfg.prefix == "sb"
assert cfg.show_scrollbar is True
assert cfg.min_thumb_height == 24
assert cfg.track_width == 3
```

``` python
# Default state
state = ScrollbarState()
assert state.position == 0
assert state.visible_count == 1
assert state.total_items == 0
```

``` python
# ID generation
ids = ScrollbarIds(prefix="cs0")
assert ids.track == "cs0-scrollbar-track"
assert ids.thumb == "cs0-scrollbar-thumb"
```
